# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node (if a bundler is added later)
node_modules/
dist/

# Models / caches (rembg, onnx)
*.onnx
.u2net/
~/.u2net/

# Local outputs / scratch
outputs/
*.local

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Claude Code — local, machine-specific overrides (launch.json is shared)
.claude/settings.local.json

# Env
.env
